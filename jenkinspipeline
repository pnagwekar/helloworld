pipeline {
  agent {
    label 'docker'
   }
   
   stages {
      stage('Hello') {
          steps {
                  sh 'java -version'
                  echo "Get Working Directory"
                  sh 'pod'
      }
    }
  }
}
